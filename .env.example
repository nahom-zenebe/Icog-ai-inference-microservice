# Optional: enables real Gemini calls. If empty, server uses a safe local mock.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-1.5-flash

# Bonus task: gRPC metadata auth
AUTH_BEARER_TOKEN=my-secret-key

# Bonus task: deadline demo (client uses 2s; server sleeps 3s by default)
SENTIMENT_DELAY_SECONDS=3
